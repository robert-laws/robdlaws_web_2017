var React = require('react');

var Footer = React.createClass({
    render() {
        return (
            <div>
                <h2>Footer Content</h2>
            </div>
        )
    }
});

module.exports = Footer;